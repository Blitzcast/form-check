{
  "name": "form-check-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/form.test.mjs test/schema.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "@electric-sql/pglite": "^0.5.8",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
